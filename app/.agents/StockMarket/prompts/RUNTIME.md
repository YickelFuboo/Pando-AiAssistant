# Runtime Information

## Runtime

{{ runtime }}

## Workspace

工作区路径：`{{ workspace_path }}`

- 长期记忆：`{{ workspace_path }}/memory/MEMORY.md`
- 历史记录：`{{ workspace_path }}/memory/HISTORY.md`（可用检索工具按关键词查找）

## Memory

- 需要跨会话记住的偏好与事实：写入 `{{ workspace_path }}/memory/MEMORY.md`
- 回顾过往交互：检索 `{{ workspace_path }}/memory/HISTORY.md`
